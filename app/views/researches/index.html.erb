<%= content_for :head do %>
  <%= stylesheet_link_tag 'researches' %>
<% end %>
<div class="">
  <%= link_to '', root_path, class: 'voltar' %>  
</div>

<div class="mb">
  <% @tanks.each do |t| %>    
      <li class="header">
        <%= link_to t.name_pt_br, research_path(t.id), class: 'botao bt-home bt-pesquisa' %>
      </li>    
  <% end %>
</div>
<%= session[:language] %> 

