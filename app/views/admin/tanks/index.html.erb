<p id="notice"><%= notice %></p>

<h1>Administrador dos Tanques</h1>

<table>
  <thead>
    <tr>
      <th colspan="3">
        Título
      </th>
    </tr>
  </thead>

  <tbody>
    <% @tanks.each do |tank| %>
      <tr>
        <td><%= tank.name_pt_br %></td>
        <td><%= link_to 'Mostrar', [:admin, tank] %></td>
        <td><%= link_to 'Editar', edit_admin_tank_path(tank) %></td>
        <td><%= link_to 'Deletar', [:admin, tank], method: :delete, data: { confirm: 'Tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Tanque', new_admin_tank_path %>
