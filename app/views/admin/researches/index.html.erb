<p id="notice"><%= notice %></p>

<h1>Administrador de Pesquisas</h1>

<table>
  <thead>
    <tr>
      <th colspan="3">
        Título
      </th>
    </tr>
  </thead>

  <tbody>
    <% @researches.each do |research| %>
      <tr>
        <td><%= research.title_pt_br %></td>
        <td><%= link_to 'Mostrar', [:admin, research] %></td>
        <td><%= link_to 'Editar', edit_admin_research_path(research) %></td>
        <td><%= link_to 'Deletar', [:admin, research], method: :delete, data: { confirm: 'Tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Pesquisa', new_admin_research_path %>
