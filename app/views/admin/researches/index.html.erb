<p id="notice"><%= notice %></p>

<h1>Admin Researches</h1>

<table>
  <thead>
    <tr>
      <th colspan="3">
        Título
      </th>
    </tr>
  </thead>

  <tbody>
    <% @researches.each do |research| %>
      <tr>
        <td><%= research.title_pt_br %></td>
        <td><%= link_to 'Show', [:admin, research] %></td>
        <td><%= link_to 'Edit', edit_admin_research_path(research) %></td>
        <td><%= link_to 'Destroy', [:admin, research], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Admin Research', new_admin_research_path %>
